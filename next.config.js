/** @type {import('next').NextConfig} */
const nextConfig = {

}

module.exports = module.exports = {
    images: {
        domains: ['dev.cleantech-mart.com'],
        unoptimized: true,
    }, experimental :{reactMode:'concurrent'},
    distDir: 'out',
  output: 'export'
}
